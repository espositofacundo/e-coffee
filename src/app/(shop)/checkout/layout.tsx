// Se puede comprar sin cuenta: el checkout solo pide nombre, teléfono y dirección.
export default function CheckoutLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
